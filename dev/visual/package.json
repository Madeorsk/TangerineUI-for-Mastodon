{
  "name": "tangerineui-visual",
  "private": true,
  "description": "Visual regression tests for the Tangerine UI theme variants.",
  "scripts": {
    "test": "playwright test",
    "update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
